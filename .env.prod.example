# ================================
# Sylion Backend - Production Environment
# ================================
# Usage:
#   1. cp .env.prod.example .env.prod
#   2. Remplir les valeurs CHANGE_ME_*
#   3. ENV_FILE=.env.prod docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build
#
# ⚠️ Ne jamais commit .env.prod !
# ================================

# ================================
# Application
# ================================
NODE_ENV=production
PORT=8000
HOST=0.0.0.0

# ================================
# Database (PostgreSQL)
# ================================
POSTGRES_DB=sylion_prod
POSTGRES_USER=sylion_user
# 🔒 SECRET OBLIGATOIRE - Générer avec: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_32_CHARS

# ================================
# Redis (Cache & BullMQ)
# ================================
# 🔒 SECRET OBLIGATOIRE - Générer avec: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_32_CHARS

# ================================
# Authentication & Security
# ================================
# 🔒 SECRET OBLIGATOIRE - Générer avec: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_RANDOM_STRING_64_CHARS_MINIMUM
JWT_EXPIRES_IN=7d

# ================================
# WhatsApp API (360dialog)
# ================================
WHATSAPP_PROVIDER=360dialog
WHATSAPP_API_URL=https://waba.360dialog.io/v1
# 🔒 SECRET OBLIGATOIRE - Depuis 360dialog dashboard
WHATSAPP_API_KEY=CHANGE_ME_360DIALOG_API_KEY
# 🔒 SECRET OBLIGATOIRE - Token de vérification webhook
WHATSAPP_VERIFY_TOKEN=CHANGE_ME_WEBHOOK_VERIFY_TOKEN_MIN_16
# 🔒 SECRET OBLIGATOIRE - ID du numéro WhatsApp
WHATSAPP_PHONE_NUMBER_ID=CHANGE_ME_PHONE_NUMBER_ID

# ================================
# Production Settings
# ================================
DEMO_MODE=false
SKIP_GCP_VALIDATION=false
LOG_LEVEL=info
LOG_PRETTY=false
ENABLE_SWAGGER=false
ENABLE_CORS=true
ENABLE_HELMET=true

# ================================
# Google Cloud Platform (Optionnel)
# ================================
# Décommenter si Vertex AI est utilisé
# GCP_PROJECT_ID=your-gcp-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/app/.secrets/gcp-service-account.json
# GCS_BUCKET_NAME=sylion-prod-bucket
# VERTEX_AI_LOCATION=us-central1
# VERTEX_AI_MODEL=gemini-1.5-pro
